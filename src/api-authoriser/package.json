{
  "name": "api-gateway-authorizer",
  "version": "1.0.0",
  "description": "AWS Lambda Authorizer for API Gateway",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "lint": "eslint .",
    "package": "zip -r authorizer.zip . -x '*.git*' 'node_modules/.cache/*' 'tests/*' '*.md'"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "eslint": "^8.0.0"
  },
  "keywords": [
    "aws",
    "lambda",
    "api-gateway",
    "authorizer",
    "authentication"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.x"
  }
}